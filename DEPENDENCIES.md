# 📦 قائمة المكتبات والاعتماديات - Khalifa Pharmacy System

## نظام إدارة محادثات صيدليات خليفة
**Created by: محمد فارس - AI Software Engineer**

---

## 📋 المكتبات الأساسية (Required)

### 1. Django Framework
```
Django==4.2.7
djangorestframework==3.14.0
```
**الوصف:** إطار العمل الأساسي للمشروع وAPI Framework

**الاستخدام:**
- بناء النظام الخلفي (Backend)
- إدارة قاعدة البيانات (ORM)
- نظام المصادقة والصلاحيات
- REST API Endpoints

---

### 2. Environment Management
```
python-dotenv==1.0.0
```
**الوصف:** إدارة المتغيرات البيئية

**الاستخدام:**
- قراءة ملف `.env`
- إخفاء المعلومات الحساسة (API Keys, Secret Keys)
- تسهيل التبديل بين بيئات التطوير والإنتاج

---

### 3. Date & Time Utilities
```
python-dateutil==2.8.2
pytz==2024.1
```
**الوصف:** معالجة التواريخ والأوقات

**الاستخدام:**
- تحويل التواريخ والأوقات
- دعم المناطق الزمنية (Egypt Timezone)
- حسابات الوقت (Response Time, Delay Tracking)

---

### 4. HTTP Requests
```
requests==2.31.0
urllib3==2.1.0
```
**الوصف:** إرسال واستقبال HTTP Requests

**الاستخدام:**
- الاتصال بـ WPPConnect API
- إرسال الرسائل عبر WhatsApp
- تحميل الصور والملفات
- Webhook Integration

---

### 5. Image Processing
```
Pillow==10.4.0
```
**الوصف:** معالجة الصور

**الاستخدام:**
- رفع وحفظ الصور
- تغيير حجم الصور
- تحويل صيغ الصور
- معالجة الصور المرسلة عبر WhatsApp

---

### 6. CORS Headers
```
django-cors-headers==4.3.1
```
**الوصف:** إدارة CORS للـ API

**الاستخدام:**
- السماح بالوصول للـ API من Frontend
- تأمين الـ API Endpoints
- دعم Cross-Origin Requests

---

## 🚀 المكتبات الاختيارية (Optional)

### للإنتاج (Production)

#### Database Drivers
```
# PostgreSQL (Recommended)
psycopg2-binary==2.9.9

# MySQL (Alternative)
mysqlclient==2.2.0
```
**متى تستخدمها:**
- عند النشر على Production
- عند الحاجة لقاعدة بيانات أقوى من SQLite
- للمشاريع الكبيرة مع عدد مستخدمين كبير

---

#### Background Tasks
```
celery==5.3.4
redis==5.0.1
```
**متى تستخدمها:**
- معالجة المهام الثقيلة في الخلفية
- إرسال الإشعارات
- معالجة قائمة الرسائل بشكل متقدم
- جدولة المهام الدورية

---

#### WebSocket Support
```
channels==4.0.0
channels-redis==4.1.0
daphne==4.0.0
```
**متى تستخدمها:**
- التحديثات الفورية (Real-time)
- إشعارات لحظية للموظفين
- تحديث التذاكر تلقائياً
- Chat في الوقت الفعلي

---

### للتطوير (Development)

#### Testing
```
pytest==7.4.3
pytest-django==4.7.0
pytest-cov==4.1.0
factory-boy==3.3.0
faker==22.0.0
```
**الاستخدام:**
- كتابة وتشغيل الاختبارات
- قياس تغطية الكود (Code Coverage)
- إنشاء بيانات وهمية للاختبار

---

#### Code Quality
```
flake8==6.1.0
black==23.12.1
isort==5.13.2
pylint==3.0.3
```
**الاستخدام:**
- فحص جودة الكود
- تنسيق الكود تلقائياً
- اكتشاف الأخطاء المحتملة
- الالتزام بمعايير Python (PEP 8)

---

#### Debugging
```
django-debug-toolbar==4.2.0
ipython==8.19.0
ipdb==0.13.13
```
**الاستخدام:**
- تتبع الأخطاء
- فحص الاستعلامات (SQL Queries)
- تحليل الأداء
- Console تفاعلي

---

## 📊 ملخص الاستخدام

### للتطوير المحلي (Development)
```bash
pip install -r requirements.txt
```
**المكتبات المثبتة:**
- Django + DRF
- python-dotenv
- python-dateutil + pytz
- requests + urllib3
- Pillow
- django-cors-headers

**الحجم التقريبي:** ~50 MB

---

### للتطوير المتقدم (Advanced Development)
```bash
pip install -r requirements-dev.txt
```
**يشمل:**
- كل مكتبات Development
- أدوات الاختبار
- أدوات فحص الكود
- أدوات التوثيق

**الحجم التقريبي:** ~150 MB

---

### للإنتاج (Production)
```bash
pip install -r requirements-prod.txt
```
**يشمل:**
- كل مكتبات Production
- Database Drivers (PostgreSQL)
- Celery + Redis
- Channels (WebSocket)
- Gunicorn (Web Server)
- Security Tools

**الحجم التقريبي:** ~200 MB

---

## 🔍 تفاصيل إضافية

### حجم المشروع الكامل
```
- Python Dependencies: ~50-200 MB (حسب البيئة)
- Node.js (WPPConnect): ~100 MB
- Database (SQLite): ~10-100 MB (حسب البيانات)
- Media Files: متغير (حسب الصور المرفوعة)
```

### متطلبات النظام
```
- RAM: 2 GB (Minimum) / 4 GB (Recommended)
- Storage: 1 GB (Minimum) / 5 GB (Recommended)
- CPU: 2 Cores (Minimum) / 4 Cores (Recommended)
- Internet: مطلوب للاتصال بـ WhatsApp
```

---

## 📝 ملاحظات مهمة

### 1. تحديث المكتبات
```bash
# عرض المكتبات القديمة
pip list --outdated

# تحديث مكتبة معينة
pip install --upgrade Django

# تحديث جميع المكتبات (غير مستحسن)
pip install --upgrade -r requirements.txt
```

### 2. حل مشاكل التثبيت

#### مشكلة Pillow على Windows:
```bash
# تثبيت Visual C++ Build Tools أولاً
# ثم:
pip install Pillow==10.4.0
```

#### مشكلة psycopg2:
```bash
# استخدم النسخة Binary
pip install psycopg2-binary==2.9.9
```

#### مشكلة mysqlclient:
```bash
# على Windows، قم بتثبيت MySQL Connector أولاً
pip install mysqlclient==2.2.0
```

### 3. البيئة الافتراضية (Virtual Environment)
```bash
# إنشاء بيئة افتراضية
python -m venv venv

# تفعيلها (Windows)
venv\Scripts\activate

# تفعيلها (Linux/Mac)
source venv/bin/activate

# التحقق من المكتبات المثبتة
pip list

# حفظ المكتبات الحالية
pip freeze > requirements-current.txt
```

---

## 🎯 التوصيات

### للمبتدئين
1. ابدأ بـ `requirements.txt` فقط
2. استخدم SQLite للتطوير
3. لا تقلق بشأن المكتبات الاختيارية

### للمحترفين
1. استخدم `requirements-dev.txt` للتطوير
2. استخدم `requirements-prod.txt` للإنتاج
3. فعّل Celery + Redis للأداء الأفضل
4. استخدم PostgreSQL بدلاً من SQLite

### للإنتاج
1. استخدم PostgreSQL أو MySQL
2. فعّل Celery للمهام الخلفية
3. استخدم Gunicorn أو uWSGI
4. فعّل Channels للتحديثات الفورية
5. استخدم Redis للـ Caching

---

## 📞 الدعم

إذا واجهت مشاكل في التثبيت:
1. تحقق من إصدار Python (3.10+)
2. تأكد من تفعيل البيئة الافتراضية
3. راجع ملف `INSTALLATION.md`
4. تواصل مع المطور: محمد فارس

---

**تم إنشاء هذا الملف بواسطة: محمد فارس - AI Software Engineer**
